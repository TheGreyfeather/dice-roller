[package]
name = "dice-roller"
version = "3.0.0"
edition = "2024"

[profile.release]
codegen-units = 1
opt-level = "z"
lto = "fat"
panic = "abort"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
rand = "0.10.2"
jiff = "0.2.35"
